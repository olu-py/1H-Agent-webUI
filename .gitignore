/target/
/.idea/
/.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Runtime state and secrets
/.1h-agent/
/.1h-agent-data/
/.runtime-data/
/.1h-agent-*.md
config.toml
.env
.env.*
*.db
*.db-shm
*.db-wal
*.log

# ts-rs default export directory (used when TS_RS_EXPORT_DIR is unset during
# cargo test; the committed bindings live in web/ts/)
/bindings/
**/bindings/

# Frontend build output
/web/node_modules/
/.pnpm-store/
*.tsbuildinfo
# web/dist/ is committed: the single Rust binary embeds it via rust-embed, so
# `cargo build` works without a prior frontend build. CI rebuilds it fresh.

# Packaging output
/dist/
*.deb
*.msi
*.zip
*.tar.gz

# GUI test evidence (not project source)
/gui-test-screenshots/
